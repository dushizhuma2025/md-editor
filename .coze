[project]
requires = ["nodejs-24"]

[dev]
build = ["pnpm", "install"]
run = ["pnpm", "--filter", "@md/web", "preview", "--port", "5000", "--host"]

[deploy]
build = ["bash", "scripts/build.sh"]
run = ["bash", "scripts/start-preview.sh"]
